# Random interfaces configuration 2
auto lo eth0 eth1

iface lo inet loopback

iface eth0 inet dhcp

iface eth1 inet dhcp

mapping eth0
    script /usr/local/bin/get-network
    map office dhcp

mapping eth1
    script /usr/local/bin/select-profile
    map vpn static
