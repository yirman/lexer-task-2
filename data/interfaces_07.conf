# Random interfaces configuration 7
auto lo eth0

iface lo inet loopback

iface eth0 inet dhcp

mapping eth0
    script /usr/local/bin/select-profile
    map office static
    map home dhcp
    map vpn fallback
