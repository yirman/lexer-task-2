# Random interfaces configuration 24
auto lo eth0

iface lo inet loopback

iface eth0 inet dhcp

mapping eth0
    script /usr/local/bin/get-network
    map office fallback
    map office loopback
    map vpn dhcp
